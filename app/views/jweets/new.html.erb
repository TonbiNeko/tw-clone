<%= form_with(model: @jweet, local: true) do |form| %>
  <div class="jweet_content">
    <%= form.label :content %>
  </div>
  <div>
    <%= form.text_area :content %>
  </div>
  <%= form.submit %>
<% end %>